# Workspace Root

This folder is now a local workspace that holds multiple projects.

## Main app

- `command_center/` — the Daily Action OS / Command Center app

## Other local folders

These are kept here for reference or separate workstreams and are not part of the main app deploy path:

- `cardhedger_sourcing_desk/`
- `ebay_auction_sniper/`
- `smart-diabetes-registry/`
- `app_legacy_20260223/`
- other archived legacy folders

## Deploy target

For GitHub-connected hosting, use `command_center` as the base directory.
